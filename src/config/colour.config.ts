import { ColourConfigType } from '@/interfaces/config'

const colourConfig: ColourConfigType = {
  /* Play around with the colours if needed,
        or view src/config/theme/palette.ts
        and uncomment the code there if you need
        more control over your colours!
    */

  primary: '#6B7D5C',
  secondary: '#6B7D5C',
}

export default colourConfig
